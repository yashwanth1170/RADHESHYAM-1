name: main

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout source code
      uses: actions/checkout@v2

    - name: Generate deployment package
      run: zip -r deploy.zip . -x '*.git*'

    - name: Deploy to EB
      uses: einaregilsson/beanstalk-deploy@v20
      with:
        aws_access_key: AKIA3RXNTXTQG7BYEXGF
        aws_secret_key: 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
        application_name: akhilyamala
        environment_name: Akhilyamala-env
        version_label: 12345
        region: us-east-2
        deployment_package: deploy.zip
